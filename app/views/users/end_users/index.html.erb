<div class="container-fluid">
    <div class="row">
		 <div class="col-md-offset-1 col-md-10">
		 	<p>ショッピングカート</p>
			<% cart_items.each do |cart_item| %>
				<%= cart_item.image_id %>
				<%= cart_item.title %>
				<%= cart_price.price * 1.08 %>
			<% end %>
		</div>
	</div>
</div>